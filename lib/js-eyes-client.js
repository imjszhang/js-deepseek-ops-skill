'use strict';

// Keep this local facade so existing imports remain stable while connection,
// authentication, timeout, and protocol compatibility are owned by JS Eyes.
module.exports = require('@js-eyes/client-sdk');
